<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<AnchorPane xmlns="http://javafx.com/javafx/23.0.1"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.blu4ck.fundickonot.controller.EditController"
            prefWidth="400" prefHeight="300">

    <VBox spacing="10">
        <padding>
            <Insets top="20" right="20" bottom="20" left="20"/>
        </padding>

        <children>
            <Label text="Not Başlığı:"/>
            <TextField fx:id="noteTitleField" promptText="Başlık girin..." />

            <Label text="Not İçeriği:"/>
            <TextArea fx:id="noteContentArea" promptText="İçeriği girin..." prefHeight="100" wrapText="true" />

            <HBox spacing="10">
                <Button fx:id="uploadImageButton"
                        text="Resim Seç"
                        onAction="#handleImageUpload"
                        style="-fx-background-color: #2196F3; -fx-text-fill: white;" />
                <Label fx:id="imagePathLabel" text="📷" />
            </HBox>

            <HBox spacing="10" alignment="CENTER_RIGHT">
                <Button text="İptal"
                        onAction="#handleCancel"
                        style="-fx-background-color: #f44336; -fx-text-fill: white;" />
                <Button text="Güncelle"
                        onAction="#handleUpdateNote"
                        defaultButton="true"
                        style="-fx-background-color: #4CAF50; -fx-text-fill: white;" />
            </HBox>
        </children>
    </VBox>
</AnchorPane>
